import {NextRequest,NextResponse} from 'next/server';import {integrationStore} from '@/lib/integrations/store';import {tenantFrom} from '@/lib/integrations/tenant';import type {IntegrationProvider} from '@/lib/integrations/types';
export async function POST(request:NextRequest,{params}:{params:Promise<{provider:string}>}){try{const {provider}=await params;const tenantId=tenantFrom(request);await integrationStore.remove(tenantId,provider as IntegrationProvider);await integrationStore.audit({tenantId,provider:provider as IntegrationProvider,action:'disconnected',at:new Date().toISOString()});return NextResponse.json({disconnected:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Disconnect failed'},{status:503})}}
