import Link from 'next/link';
import { CalendarClock, Instagram, Link2, MessageSquareText } from 'lucide-react';
import { Button } from '@/components/ui/button';
const CHANNELS=[{name:'Instagram',detail:'Posts, Reels and performance',icon:Instagram},{name:'Facebook',detail:'Page posts and event promotion',icon:MessageSquareText},{name:'Content calendar',detail:'Plan once across connected channels',icon:CalendarClock}];
export default function SocialPage(){return <div className="space-y-5"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-bold">Social media</h2><p className="mt-1 text-sm text-muted-foreground">Plan organic content, then connect channels when their OAuth flows are ready.</p></div><Button asChild variant="outline"><Link href="/dashboard/settings/integrations"><Link2 className="mr-2 h-4 w-4"/>Manage connections</Link></Button></div><div className="overflow-hidden rounded-xl border bg-white">{CHANNELS.map(({name,detail,icon:Icon})=><div key={name} className="flex items-center gap-4 border-b p-4 last:border-0"><span className="rounded-lg bg-slate-100 p-3"><Icon className="h-5 w-5"/></span><div className="flex-1"><p className="font-semibold">{name}</p><p className="text-sm text-muted-foreground">{detail}</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium">Not connected</span></div>)}</div></div>}
