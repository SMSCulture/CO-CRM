'use client';
import { useState } from 'react';
import { CheckCircle2, Circle, Plus, UserRound } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Textarea } from '@/components/ui/textarea';
import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle } from '@/components/ui/dialog';
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
import { Tabs,TabsList,TabsTrigger } from '@/components/ui/tabs';
import type { WorkKind } from './task-types';
import { WORK_TYPE_META } from './task-types';

type Task={id:number;contact:string;title:string;due:string;kind:WorkKind;subtype:string;done:boolean};
const seed:Task[]=[{id:1,contact:'Maya Rodriguez',title:'Follow up after first visit',due:'Today',kind:'stewardship',subtype:'Follow-up',done:false},{id:2,contact:'Jordan Lee',title:'Confirm accessibility preference',due:'Today',kind:'service',subtype:'Accessibility',done:false},{id:3,contact:'Alex Morgan',title:'Review post-event feedback',due:'Tomorrow',kind:'survey',subtype:'Post-event feedback',done:false}];
export default function TasksPage(){const[tasks,setTasks]=useState(seed);const[kind,setKind]=useState<'all'|WorkKind>('all');const[open,setOpen]=useState(false);const[title,setTitle]=useState('');const[contact,setContact]=useState('');const[newKind,setNewKind]=useState<WorkKind>('stewardship');const[due,setDue]=useState('Today');const visible=kind==='all'?tasks:tasks.filter(t=>t.kind===kind);function create(){if(!title.trim())return;setTasks(items=>[{id:Date.now(),contact:contact.trim()||'Unassigned',title:title.trim(),due,kind:newKind,subtype:WORK_TYPE_META[newKind].label,done:false},...items]);setTitle('');setContact('');setOpen(false)}return <div className="space-y-6"><div className="flex items-center justify-between"><div><h2 className="text-2xl font-bold">Tasks & service</h2><p className="mt-1 text-sm text-muted-foreground">Stewardship, cases and survey follow-up without automatic sends.</p></div><Button className="gap-2" onClick={()=>setOpen(true)}><Plus className="h-4 w-4"/>New task</Button></div><Tabs value={kind} onValueChange={value=>setKind(value as typeof kind)}><TabsList><TabsTrigger value="all">All</TabsTrigger><TabsTrigger value="stewardship">Stewardship</TabsTrigger><TabsTrigger value="service">Service</TabsTrigger><TabsTrigger value="survey">Survey</TabsTrigger></TabsList></Tabs><div className="overflow-hidden rounded-xl border bg-white">{visible.map(task=><div key={task.id} className="flex min-h-14 items-center gap-4 border-b px-4 py-3 last:border-b-0"><button onClick={()=>setTasks(items=>items.map(item=>item.id===task.id?{...item,done:!item.done}:item))} aria-label={task.done?'Reopen task':'Complete task'}>{task.done?<CheckCircle2 className="h-5 w-5 text-emerald-600"/>:<Circle className="h-5 w-5 text-slate-400"/>}</button><div className="min-w-0 flex-1"><p className={task.done?'text-sm line-through text-muted-foreground':'text-sm font-semibold'}>{task.title}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><UserRound className="h-3 w-3"/>{task.contact} · {WORK_TYPE_META[task.kind].label}</p></div><Badge variant="outline">{task.subtype}</Badge><span className="w-20 text-right text-sm text-muted-foreground">{task.due}</span></div>)}</div><Dialog open={open} onOpenChange={setOpen}><DialogContent><DialogHeader><DialogTitle>Create task</DialogTitle><DialogDescription>Add a staff-owned follow-up. Nothing is sent to the patron.</DialogDescription></DialogHeader><div className="space-y-4"><div className="space-y-1.5"><Label>Task</Label><Textarea value={title} onChange={e=>setTitle(e.target.value)} placeholder="What needs to happen?"/></div><div className="space-y-1.5"><Label>Patron or owner</Label><Input value={contact} onChange={e=>setContact(e.target.value)} placeholder="Search or enter a name"/></div><div className="grid grid-cols-2 gap-3"><div className="space-y-1.5"><Label>Type</Label><Select value={newKind} onValueChange={v=>setNewKind(v as WorkKind)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="stewardship">Stewardship</SelectItem><SelectItem value="service">Service</SelectItem><SelectItem value="survey">Survey</SelectItem></SelectContent></Select></div><div className="space-y-1.5"><Label>Due</Label><Select value={due} onValueChange={setDue}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="Today">Today</SelectItem><SelectItem value="Tomorrow">Tomorrow</SelectItem><SelectItem value="This week">This week</SelectItem></SelectContent></Select></div></div></div><DialogFooter><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={create} disabled={!title.trim()}>Create task</Button></DialogFooter></DialogContent></Dialog></div>}
