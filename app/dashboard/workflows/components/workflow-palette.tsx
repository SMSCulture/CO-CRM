'use client';
import { useState } from 'react';
import { Bell, CalendarClock, CircleStop, Clock3, Facebook, Flag, GitBranch, Instagram, Mail, Megaphone, Search, TicketPercent, Tags, UserRoundCog, Webhook } from 'lucide-react';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Input } from '@/components/ui/input';
import type { WorkflowNodeType } from '@/store/workflow-builder-store';

export interface PaletteItem { type: WorkflowNodeType; subtype: string; label: string }
const GROUPS = [
 { label:'Start', hint:'CRM activity', items:[{type:'trigger',subtype:'record_created',label:'CRM record created',icon:Webhook,tone:'text-sky-600 bg-sky-50'},{type:'trigger',subtype:'event_published',label:'Event published',icon:CalendarClock,tone:'text-violet-600 bg-violet-50'}]},
 { label:'Communicate', hint:'Messages and channels', items:[{type:'action',subtype:'send_email',label:'Send email',icon:Mail,tone:'text-blue-600 bg-blue-50'},{type:'action',subtype:'notify_staff',label:'Notify staff',icon:Bell,tone:'text-amber-600 bg-amber-50'},{type:'action',subtype:'instagram',label:'Instagram',icon:Instagram,tone:'text-fuchsia-600 bg-fuchsia-50'},{type:'action',subtype:'facebook',label:'Facebook',icon:Facebook,tone:'text-[#1877F2] bg-blue-50'},{type:'action',subtype:'tiktok',label:'TikTok',icon:Megaphone,tone:'text-cyan-600 bg-cyan-50'}]},
 { label:'Update CRM', hint:'Patron data', items:[{type:'action',subtype:'add_to_segment',label:'Add to segment',icon:Tags,tone:'text-emerald-600 bg-emerald-50'},{type:'action',subtype:'update_field',label:'Update contact field',icon:UserRoundCog,tone:'text-indigo-600 bg-indigo-50'},{type:'action',subtype:'promo_code',label:'Create promo code',icon:TicketPercent,tone:'text-rose-600 bg-rose-50'}]},
 { label:'Control', hint:'Path and timing', items:[{type:'condition',subtype:'contact_filter',label:'If / else',icon:GitBranch,tone:'text-violet-600 bg-violet-50'},{type:'wait',subtype:'fixed_delay',label:'Wait',icon:Clock3,tone:'text-slate-600 bg-slate-100'},{type:'goal',subtype:'conversion',label:'Goal',icon:Flag,tone:'text-emerald-600 bg-emerald-50'},{type:'exit',subtype:'complete',label:'Exit',icon:CircleStop,tone:'text-slate-600 bg-slate-100'}]},
] satisfies Array<{label:string;hint:string;items:Array<PaletteItem & {icon:typeof Mail;tone:string}>}>;

export function WorkflowPalettePanel({onAdd,compact=false}:{onAdd:(item:PaletteItem)=>void;compact?:boolean}){
 const [activeGroup,setActiveGroup]=useState(GROUPS[0].label); const [search,setSearch]=useState(''); const group=GROUPS.find(item=>item.label===activeGroup)??GROUPS[0];
 const items=group.items.filter(item=>item.label.toLowerCase().includes(search.toLowerCase()));
 return <div className="flex h-full min-h-0 flex-col"><div className="relative mb-3"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><Input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Find a node" className="h-9 pl-9"/></div><div className={`grid gap-3 ${compact?'':'sm:grid-cols-[180px_1fr]'}`}><nav className={`gap-1 ${compact?'grid grid-cols-2':'space-y-1'}`} aria-label="Node categories">{GROUPS.map(item=><button key={item.label} onClick={()=>{setActiveGroup(item.label);setSearch('')}} className={`rounded-md px-2.5 py-2 text-left ${activeGroup===item.label?'bg-sky-50 text-co-blue':'hover:bg-slate-50'}`}><span className="block text-xs font-semibold">{item.label}</span><span className="block text-[11px] text-muted-foreground">{item.hint}</span></button>)}</nav><section className="min-w-0"><p className="mb-2 text-xs font-semibold uppercase tracking-wide text-muted-foreground">{group.label}</p><div className="overflow-hidden rounded-lg border bg-white">{items.map(item=>{const Icon=item.icon;return <button key={item.subtype} onClick={()=>onAdd(item)} className="flex w-full items-center gap-2.5 border-b p-2.5 text-left last:border-b-0 hover:bg-slate-50"><span className={`rounded-md p-1.5 ${item.tone}`}><Icon className="h-4 w-4"/></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-medium">{item.label}</span>{['instagram','facebook','tiktok'].includes(item.subtype)&&<span className="block text-[11px] text-muted-foreground">Organic / paid options</span>}</span><span className="text-xs font-medium text-co-blue">+</span></button>})}</div></section></div></div>
}

export function WorkflowNodePicker({open,onOpenChange,onAdd}:{open:boolean;onOpenChange:(open:boolean)=>void;onAdd:(item:PaletteItem)=>void}){
 return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[82vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Add a node</DialogTitle><DialogDescription>Choose a node. Its setup opens next.</DialogDescription></DialogHeader><WorkflowPalettePanel onAdd={item=>{onAdd(item);onOpenChange(false)}}/></DialogContent></Dialog>
}
