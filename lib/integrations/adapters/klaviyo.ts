import type {ProviderAdapter} from '../types';
async function request(key:string,path:string){const response=await fetch(`https://a.klaviyo.com/api${path}`,{headers:{authorization:`Klaviyo-API-Key ${key}`,accept:'application/vnd.api+json',revision:'2025-07-15'},cache:'no-store'});if(!response.ok)throw new Error(`Klaviyo verification failed (${response.status})`);return response.json()}
export const klaviyoAdapter:ProviderAdapter={provider:'klaviyo',async verify(key){await request(key,'/accounts/');return {externalAccountId:'klaviyo-account'}},async discoverAssets(key){const data=await request(key,'/lists/?page[size]=100') as {data?:{id:string;attributes?:{name?:string}}[]};return (data.data||[]).map(list=>({id:`list:${list.id}`,type:'list',name:list.attributes?.name||list.id,externalId:list.id}))},async health(key){try{await request(key,'/accounts/');return 'connected'}catch{return 'revoked'}}};
